{
  "name": "filedrop",
  "private": true,
  "workspaces": [
    "web",
    "ws"
  ],
  "scripts": {
    "start": "WS_USE_PROXY=1 nx run-many -t start -p web ws",
    "build": "nx run-many -t build -p web ws",
    "start:prod": "cd ws && yarn start:prod",
    "test": "nx run-many -t test -p ws"
  },
  "devDependencies": {
    "eslint": "^8.35.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.1.2",
    "prettier": "^2.8.4",
    "nx": "16.5.5"
  }
}
